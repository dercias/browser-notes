import styled from 'styled-components/macro';
import tw from 'twin.macro';

export const NoteToolbarContainer = styled.div(
  tw`w-full p-1 flex align-middle bg-white`
);

export const Toolbar = styled.div(tw`w-full px-10 flex justify-between`);

export const ToolbarStart = styled.div(tw``);

export const ToolbarEnd = styled.div(tw``);
