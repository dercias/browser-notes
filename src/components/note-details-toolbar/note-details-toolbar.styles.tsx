import styled from 'styled-components/macro';
import tw from 'twin.macro';

export const NoteDetailsToolbarContainer = styled.div(
  tw`w-full h-10 border-neutral-200 border-b bg-white px-10 flex align-middle`
);
